<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <data noupdate="1">
        <!--Email template -->
        <record id="email_template_payslip" model="mail.template">
            <field name="name">Payslip: Send by email</field>
            <field name="model_id" ref="simplit_hr_overtime.model_hr_paroll"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">${object.employee_id.name} Payslip</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Dear ${object.employee_id.name}
        <br /><br />
        Here is your 
        Payslip
        <br /><br />
        Do not hesitate to contact us if you have any question.
    </p>
</div>
            </field>
          <!--   <field name="report_template" ref="account_invoices"/>
            <field name="report_name">Payslip_${(object.employee_id.name}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/> -->
        </record>
    </data>
</odoo>
 
